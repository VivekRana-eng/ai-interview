{
  "name": "ai-selection-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend -- -p 3000",
    "dev:backend": "npm run dev --prefix backend"
  }
}
